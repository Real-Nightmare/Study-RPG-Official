# Academics Module

Defines the academic study structure — **subjects → chapters → topics** — that the RPG
layer and every study feature hang off.

- **HTTP**: `@Controller('academics')`
- **Key service**: `AcademicsService`
