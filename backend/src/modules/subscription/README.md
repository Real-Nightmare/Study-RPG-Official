# Subscription Module

Stripe subscriptions via `@Controller('subscription')` and the `webhooks` controller.

> **Free-to-Win**: subscriptions gate infrastructure only (limits, priority). They can
> never buy XP, loot, cosmetics or skip study requirements.

- **Key service**: `SubscriptionService`; webhook: `StripeWebhookController`
