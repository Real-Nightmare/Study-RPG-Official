# Events Module

Study events — **StudyPass**, quests, and the lore events (Abstracted, Great Extinction).
Event rewards follow the wellbeing diminishing-returns curve.

- **HTTP**: `@Controller('events')`
- **Key service**: `AbstractedService`, `EventsService`
