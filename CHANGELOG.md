# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- **Clean-room rewrite program (owner: "rewrite all the files not by us and after rewriting everything we remove the license")** — `docs/audits/REWRITE_LEDGER.md` tracks all **405 upstream Studyield files** with a per-file disposition (REWRITE/DELETE/REPLACE/KEEP/SCHEMA). Completed so far: root & infra (CI, issue/PR templates, SECURITY, CoC, CONTRIBUTING, README, docker-compose, start.sh, FUTURE_GOAL rewritten as original Study RPG docs; 12 upstream-branded translated READMEs + Studyield PDF removed), branding assets (STUDYIELD2.png, studyield-logo.png, screenshots, Vite template assets deleted; new original `study-rpg-logo.svg` + favicon + 10 component references updated), the backend common layer (decorators, filters, guards, interceptors, gateways — same contracts, original implementations; barrels/wiring kept as functional), and **B4: all 130 upstream backend module files** (infra, account, content/AI, and interactive module services/controllers/gateways reimplemented from scratch with identical contracts; Study RPG behavior — integrity rewards, anti-slop guards, philosophy prompts, faction/XP paths — preserved throughout; declarative controllers, Nest wiring, DTOs and prompt-contract agents kept as functional). Brand-leak sweep: `web-push`/`admin` email defaults → `@studyrpg.app`, API title → Study RPG API. Backend still **501/501 tests** + build + lint green. **The AGPL-3.0 LICENSE/NOTICE removal is gated**: it happens only when the ledger shows zero unresolved upstream files — see `LICENSE_AUDIT.md` §7 (2026-08-16 entry)
- **Ocean Protocol Compute-to-Data (C2D) — real on-chain publishing (owner follow-up: "implement Ocean Protocol's Compute-to-Data feature cause I have 2 MATIC!")** — `ocean-c2d.service.ts` uses the official Ocean.js SDK (`@oceanprotocol/lib` 8.6.2 — the library the Ocean CLI is built on, ethers v6) to publish marketplace datasets as genuine C2D assets: deploys an **ERC721 data NFT + ERC20 datatoken** (with a **fixed-rate exchange** bundled when the dataset has a price, so buyers swap OCEAN → datatoken), **encrypts the aggregate file URL** with the Ocean Node (`ProviderInstance.encrypt`), builds a DDO with a **`compute` service** (privacy policy stamped in: raw algorithms on/off, network access off by default to block exfiltration, optional algorithm-publisher allowlist), and **stores the DDO on-chain** (ERC725 metadata via `Nft.setMetadata` after `Aquarius.validate`). Defaults target **Polygon mainnet (chain 137)** — `OCEAN_ERC721_FACTORY`/`OCEAN_FIXED_RATE_EXCHANGE`/`OCEAN_TOKEN_ADDRESS` default to Ocean's Polygon deployment so the owner's **2 MATIC covers gas**; every address + RPC is env-overridable for other supported chains (e.g. Base). `MarketplaceService.publishDataset` now uploads the sanitized aggregate JSON to R2 (needs `R2_PUBLIC_URL`) and attempts C2D when a wallet + RPC + node are configured; **any C2D failure or missing config gracefully falls back to metadata-first publishing** (dataset still becomes discoverable, `c2d_error` recorded for admins). Migration `031_study_c2d.sql` (`nft_address`, `datatoken_address`, `exchange_id`, `provider_url`, `c2d_policy`, `aggregate_file_key`, `c2d_error`); admin `GET /data-marketplace/status` now reports `c2d` readiness; publish accepts an optional per-dataset `c2d` policy override; `DatasetView` exposes the on-chain artifact addresses. **Important 2026 ecosystem note**: the legacy `aquarius.mainnet.oceanprotocol.com` endpoint is retired — Ocean now runs on Ocean Nodes (`OCEAN_NODE_URL` default `https://compute1.oceanprotocol.com/`); env vars documented in `docs/getting-started/configuration.md` and mirrored in `backend/.env.example`; `THIRD_PARTY_NOTICES.md` updated. 2 new/updated test suites (backend suite now **501 tests**)
- **Upstream file inventory** — `docs/audits/UPSTREAM_FILE_INVENTORY.md` exhaustively maps the 405 files imported from upstream Studyield (commit `0494e1a`), marks the 133 later modified by Study RPG work, and documents the 459 new original files; links from `UPSTREAM.md` for provenance auditing
- **Study RPG — Data Marketplace & AI Effectiveness Benchmark (Spec Kit spec `016-study-data-marketplace`)** — owner brief: keep Study RPG sustainable by selling *educational data* through the Ocean Protocol ecosystem without ever leaking student credentials, plus an admin AI benchmarking pipeline that scores how much studying with Study RPG improves outcomes. **(1) Privacy-first data marketplace** — migration `030_study_data_marketplace.sql` (`data_consent` with revocable opt-in, `marketplace_datasets`, `benchmark_runs`); `MarketplaceService` owns consent (audited) and dataset CRUD (audited) with publish/revoke under `/data-marketplace`; **publish is aggregates-only and consent-gated**: aggregates are computed over the consenting cohort only (allowlisted country/board/grade filters), every output field passes `privacy-guard.ts` (aggregate prefix/suffix rules + PII/sensitive blocklist + minimum group size + ≥80% consent coverage), the exact payload is SHA-256 checksummed, and `OceanService` builds a standard Ocean DDO (`did:op` from the metadata hash, OEP-08-style metadata, checksummed `files` entry) and POSTs it to Aquarius (`OCEAN_AQUARIUS_URL`; never blocks on network errors — the DDO is stored for re-submission/export; on-chain datatoken minting is a documented follow-up needing a funded wallet). **(2) Admin AI effectiveness benchmarking** — `BenchmarkService` compares two consecutive study windows (admin picks N days + optional cohort filters), pure `benchmark-metrics.ts` computes per-metric deltas + a weighted 0–100 effectiveness score + verdict band (transformative/strong/moderate/neutral/negative), and an AI narrative grounded ONLY in those numbers (deterministic fallback when AI is unavailable) is persisted; admin-only `POST/GET /data-marketplace/benchmarks*`, reports never touch the marketplace and never reference individual students. Frontend: **AdminPage Data & Benchmarks tab** (run/list benchmarks with deltas + score + AI report; dataset create/publish/revoke/delete with reason prompts and the privacy report) and a **Data & Privacy consent toggle** in Account Settings; `dataMarketplace` service + types + endpoints; `admin.tab.*`, `admin.benchmarks.*`, `admin.datasets.*`, `accountSettingsPage.*` locale keys in all 15 locales. 6 new test suites (`privacy-guard`, `benchmark-metrics`, `ocean.service`, `marketplace.service`, `ocean-node-policy`, `ocean-node-monitor` — the last two cover the **idle-capacity Ocean Node**: when the server is fully idle it runs an official `oceanprotocol/ocean-node` container that earns provider fees, stopping the moment any user appears, with a post-stop cooldown + rolling daily start cap to prevent flapping) — backend suite now **491 tests / 61 suites**; env vars documented in `docs/getting-started/configuration.md`
- **Cloudflare Pages auto-deploy + free static frontend serving** — pushing to `main` now runs `.github/workflows/deploy-frontend-cloudflare.yml` (build `frontend/dist`, upload with wrangler-action; secrets `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`, repo vars `CLOUDFLARE_PROJECT_NAME`/`VITE_API_URL`); `frontend/wrangler.toml` (Pages config), `public/_redirects` (SPA fallback), `public/_headers` (hashed assets immutable 1y, `index.html`/`sw.js` revalidated) and `public/_routes.json` (exclude-all Functions guard) — the frontend is served as a **pure static site**: per Cloudflare's pricing, static asset requests on Pages are **free and unlimited** and never consume the shared **Workers + Pages Functions** 100k/day quota (the `_routes.json` guard keeps every request in the static pool even if a `functions/` dir is added later; no Functions, no Worker proxy — the API talks to the NestJS backend directly); docs in `docs/deployment/hosting.md` (Option C) + `frontend/README.md`
- **CI now runs tests + coverage** (`.github/workflows/ci.yml` — backend `npm run test:cov -- --runInBand` with coverage artifact, frontend `npm test`); backend `npm run lint` restored (formatted + fixed `prefer-const` across `src` so lint is green: 0 errors / 30 warnings)
- **Frontend a11y pass** — `eslint-plugin-jsx-a11y` (flat recommended + i18n-HTML label rule) wired into `frontend/eslint.config.js`, accessible-label fixes across dashboard pages/components, `src/lib/a11y.ts` + unit tests
- **Backend quality refactor** — shared `common/config/cors-origins.ts` (`parseCorsOrigins` + unit tests) used by `main.ts` and all WebSocket gateways; health/root version from `ConfigService`; `joi` added for config validation
- **Docs wave** — `docs/architecture/overview.md`, `docs/getting-started/` (quick-start, configuration), `docs/guides/connector-guide.md`, `docs/STUDY_RPG_PHILOSOPHY.md`, per-module READMEs for every backend module, ADR-0001 licence decision
- **Study RPG — Anti-OverStudy & Health-First Wellbeing (Spec Kit spec `015-study-wellbeing`)** — owner follow-up: "the AI must use the philosophy too" + "add anti-OverStudy mechanisms to heavily dampen OverStudy and promote smarter studying". **(1) Every AI surface now speaks the canonical product philosophy** (`ai/study-rpg-philosophy.ts` — depth over length, mastery over memorisation, **health-first anti-overstudy guardian** that refuses to encourage cramming and steers tired/late-night students toward rest, Free-to-Win, game-to-reality framing, honest evidence-based tone, Socratic style) injected into the chat assistant, Feynman teach-back evaluator, campfire tutor (question + depth grading), programme architect, and learning-path coach. **(2) Anti-overstudy dampening** (`integrity/overstudy.ts`, pure + unit-tested — linear diminishing returns 1.0 → floor past the healthy daily optimum and heavily dampened at the hard cap, rest-cooldown gate after long blocks, IST night-rest guard, study-health bands fresh/focused/draining/depleted): `FocusSessionsService.start` gates new sessions during an active rest cooldown/exhaustion and soft-nudges during night-rest hours (`ackNightRest`), `complete` applies the overstudy decay factor, and `StudyEventsService.recordStudyActivity` scales event EXP by it — real study is still rewarded, grinding is heavily dampened; migration `029_study_wellbeing.sql` (`game_config` `rpg.wellbeing` seed, defaults mirrored in `integrity-config.ts`); frontend `GET /focus-sessions/study-health` + **`FocusSessionsPage` study-health meter** (band colour/label/multiplier), rest-cooldown/exhausted start gate, night-rest nudge, dampened-reward notice, full `wellbeing` locale namespace in all 15 locales; 1 new test suite (backend suite now **430 tests**)
- **Study RPG Integrity — F2W Meritocracy & Anti-Cheese (Spec Kit spec `014-study-integrity`)** — the owner's 5-mandate brief, wired end-to-end across the reward paths: **exponential reward curve** (`integrity/reward-curve.ts` — accuracy factor 0 below 60% with a quadratic/exponential tail to 3.5× at 100%, focus-consistency ≤1.5×, material difficulty 1.0/1.5/2.0, campfire 1.0–1.5×; STP premium thresholds quiz ≥90 / exam ≥80 / teach-back ≥70 with daily caps); **anti-cheese behavioural guards** (`integrity/behavior-guard.ts` — sliding-window rate limit, min 4 s/question answer-time sanity, server-clock focus-session verification with engagement proof + idle throttling at 0.35× + inflated-claim rejection, daily focus cap 240 min); **enforcement in all five reward paths** (focus-sessions ignores client minute overrides, quiz 12 attempts/hour + time sanity, exam 5 attempts/day + time sanity, teach-back min 80-char explanation + level-aware XP, battles scale by academic accuracy with integrity floor 0.6 + premium ≤2.0×); **metacognitive Campfire loop** (`integrity/campfire.service.ts` + controller — AI tutor asks ONE targeted synthesis question before rewards are cashed in, depth-graded 0–100 via AI with a deterministic lexical fallback, 1.0–1.5× multiplier persisted, daily cap 3, idempotent per user/day/source); migration `028_study_integrity.sql` (`campfire_reflections` + `focus_sessions.verification` + `rpg.integrity` seed); **zero pay-to-win** (no purchase of STP with real money anywhere — premium rewards are academic-achievement-gated only); frontend `services/studyIntegrity.ts` + types + `api.ts` endpoints, **`CampfireReflectionModal`** integrated into `StudySessionPage` + `RpgPage` with mastery-framed copy ("Your real-world cognitive capacity and focus endurance have increased…"), full `campfire` locale namespace in all 15 locales; 3 new test suites (backend suite now **410 tests**)
- PDF Phase 9 — Hardening (Spec Kit spec `013-study-hardening`): **audit-log export/retention/purge** (`AuditService.exportCsv/exportJson`, `getRetention`/`setRetention` persisted to `audit_retention_config`, `purgeOlderThan` under advisory lock, BullMQ `audit-retention` worker, admin endpoints); **DM moderation + rate limits** (`social/dm-moderation.ts` — blocked-word hits, verdicts, per-user per-minute rate limit, audited `social.dm_moderated`/`social.dm_rate_limited`); standards-based **Web Push (VAPID)** (`web-push.service.ts`, graceful no-op when unconfigured, subscribe/unsubscribe/public-key endpoints, `frontend/public/sw.js` + `NotificationSettingsPage` push block); **faction settlement** on a repeatable daily BullMQ job (`faction-settlement`, IST-period idempotent); **admin system status** (`AdminService.status()` — users by role, audit count, active events/factions, queue stats, db/redis health); migration `027_study_hardening.sql`; ops scripts (`scripts/backup.sh`, `scripts/restore.sh`, `scripts/load-test.mjs`) + runbooks (`docs/runbooks/backup-restore.md`, `audit-retention.md`, `load-testing.md`) + deployment guide (`docs/deployment/hosting.md`); `AdminPage` System tab + audit export/retention/purge UI + `admin`/`notif.webPush` locale keys in all 15 locales; 3 new test suites (backend suite now **373 tests**)
- PDF Phase 8 — Advanced Learning (Spec Kit spec `012-study-advanced-learning`): **programme templates** (`programme_templates` table, admin CRUD with required reason + audit, `suggestFromTemplate` one-click instantiate reusing the AI build path); **AI review queue + batch review** (`reviewQueue()` for programmes without a human verdict, `batchReview` per-item with audit + history, `adminReview`/`reviewWithAi` append to capped `review_history`); **AI programmes → personal learning paths** (`LearningPathsService.generateFromProgramme` — active programmes only, objectives/milestones/reward policy mapped into 8–14 ordered steps, AI self-review with score/verdict/reasons and `needs_regeneration` flag when score < 60, linked `programme_id`, graceful failure fallback); migration `026_study_advanced.sql`; endpoints `GET /programmes/templates` · `POST/PATCH/DELETE` (admin) · `POST /programmes/templates/:id/suggest` · `GET /programmes/review-queue` · `POST /programmes/batch-review` · `POST /programmes/:id/learning-path`; `ProgrammesPage` templates strip + review queue tab, `LearningPathsPage` programme badge + regenerate CTA, full `programmes`/`learningPaths` locale namespaces in all 15 locales; 3 new test suites (backend suite now **373 tests**)
- PDF Phase 7 — Study Events (Spec Kit spec `011-study-events`): always-active event scheduler (lazy scheduled→active→ended transitions; **Study Sprint** fallback created atomically under a Postgres advisory lock when no event is scheduled, admins warned before activation); **StudyPass** with 14 cumulative EXP thresholds (0/100/…/1750) stored separately from player XP and accruing only from real study activity (tasks, focus, quizzes, puzzles, battles — `recordStudyActivity` hooked into all five services); **Free/Gold tracks** (Gold 1500 SLC, idempotent debit, locks before first claim, mutually exclusive, no double claims); data-driven **quests** (daily/weekly/study/puzzle with IST day / ISO week period keys, single-claim STP/event-EXP/item rewards); the **Abstracted** event (original 40-Mana Abstracted cards, irreversible unabstracting → Legendary + 1 Abstracted Error + 500 STP with audited reason, 7 Errors → untradeable **Limbo** Legendary once); the **Great Extinction** (exactly 10 targets seeded 5 Common→Rare + 5 underused Legendaries, targeted burns grant tradeable **Extinction Sigils** + global pyre milestone, friend-only Sigil transfers, preserve-a-card quests via Sigil consumption); `CardService.grantEventCard` + `openLootBox` (weighted rarity with published odds); migration `025_study_events.sql` + `game_config` `rpg.events` seed; `EventsModule` (@Global) endpoints incl. admin create/activate (RolesGuard, audited); **`EventsPage`** (event banner, StudyPass progress + 14 pips + track picker + claims, quests, items/sigils, Abstracted + Limbo, extinction targets + milestone, admin scheduling) wired into router + nav (`nav.events`, full `events` locale namespace in all 15 locales); 8 new test suites (backend suite now **302 tests**)
- PDF Phase 6 — Study RPG Economy (Spec Kit spec `010-study-economy`): supply-tied official card value (`card-value.ts` rarity base × supply multiplier with decay floor) and supply reconciliation with extinction + `Echo of …` replacement print runs (`supply.ts`, `checkExtinction`); marketplace (list cards, atomic buys, offers accept/decline/cancel, 24h expiry) and inventory/vault moves via `EconomyService`; scraper (permanent removal, immediate official-value payout) and burner (payout in instalments — first now, rest on schedule, `burn-instalments.ts` + `processDueInstalments`); migration `024_study_economy.sql` (`card_supply_ledger`, `card_price_history`, `marketplace_listings/offers`, `card_burn_instalments`, `game_config` economy seed); `EconomyModule` endpoints + admin reconcile/process-instalments (RolesGuard); `EconomyPage` (Marketplace / My Cards / Supply tabs) wired into router + nav (all 15 locales); 54 economy unit tests (backend suite 256 total)
- Adopted **GitHub Spec Kit** (Specify CLI 0.16.0, GitHub Copilot integration): `.specify/` tooling (templates/scripts/workflows), 10 `/speckit.*` skills under `.github/skills/`, project constitution at `.specify/memory/constitution.md`, and a spec-first workspace at `specs/` with 10 feature specs migrated from OpenSpec (including the in-progress Phase 6 Economy, `010-study-economy`)
- Migrated the OpenSpec workspace to Spec Kit and archived it at `archive/openspec/` (specs, changes, config, and agent hooks) — new features are authored spec-first via `/speckit.*` commands
- Phase 5 (PvP duels, Spec Kit spec `008-pvp-duels`): async Seeker duels — challenge a player by email or battle-rating matchmaking (each side fights a deterministic ghost avatar of the opponent's deck snapshot through the existing engine, so outcomes are fair and fully replayable), Elo battle rating with floor, transactional idempotent settlement (decisive → HP% → turns → draw, expiry defaults the unplayed side), STP/XP rewards with daily anti-farming cap, and a rating leaderboard; migration `022_study_pvp.sql` (`pvp_duels` + `battles.pvp_duel_id` + `game_config` `rpg.pvp` seed); `PvpService` + `POST/GET /rpg/pvp/duels`, `GET /rpg/pvp/duels/:id`, `POST /rpg/pvp/duels/:id/battle`, `GET /rpg/pvp/leaderboard`; `RpgPage` Duel tab (challenge by email / random, duel list, ghost battle, leaderboard — all 15 locales); 10 PvP unit tests (backend suite 166 total)
- Phase 4 (Study RPG core, Spec Kit spec `007-study-rpg-core`): config-driven player stats/levels (`player_profiles` + `game_config` thresholds), immutable **STP/SLC** wallet ledger (single currency, integer-only, locked mutations, idempotency keys, no negative balances), original data-driven cards with per-user ownership + 5-card restricted decks (one Poison/Decay/Shield/Silence each), deterministic server-authoritative battle engine with replayable logs (shields, statuses, mana quiz +4/cap 20, one-turn +10 damage challenge, DoT separated from immediate damage), and win rewards with anti-farming daily limits; migration `021_study_rpg.sql`; `RpgModule` (profile/ledger/cards/decks/battles endpoints) + `RpgPage` with Character/Decks/Battle tabs wired into router + nav (all 15 locales); 35 new unit tests (backend suite 139 total)
- Phase 2 Study Tools completion (Spec Kit spec `001-studyield-core`): focus sessions (start/pause/resume/complete with task/subject links, today summary), mistake notebook (categories, cause, resolve/reopen lifecycle, status counts), subject puzzles (ranked vs practice, per-subject streaks with shields, personal best, attempt history, daily ranked limit, no immediate reuse), and exam periods/exam centre (derived status, attach exams, record results with mistake analysis + revision plan, nearest exam); migration `020_study_tools.sql`; four new dashboard pages wired into router + nav (all 15 locales)
- Phase 2 dashboard v2 (Spec Kit spec `002-student-dashboard`): `DashboardModule` with `GET /dashboard/summary` aggregate (today plan, tasks due, upcoming exams, exam portions, focus minutes, flashcards due, quiz accuracy, recent mistakes, weak topics, puzzle + study streaks, game stats, daily quests, rule-based recommended next action) and `GET/PUT /dashboard/preferences` with persisted **hide-game-stats** (game stats omitted from the payload when enabled); `DashboardHomePage` reworked into a live widget grid with the toggle
- Phase 3 (Production RAG): deletion pipeline — `DocumentDeletionService` walks the ingestion state machine (`deleting→deleted`, refuses mid-ingestion, clears `kb_chunks` + active Qdrant collection points, failure rollback to `failed`), `DELETE /knowledge-bases/:id/documents/:docId` endpoint
- Phase 3 (Production RAG): reranker — `RerankProvider` abstraction (OpenRouter / OpenAI-compatible / Ollama, env-selected via `RERANKER_PROVIDER`/`MODEL`/`API_URL`/`API_KEY`), optional rerank stage in hybrid search (`rerank`, `rerankTopK`) with graceful fallback to fusion order
- Phase 3 (Production RAG): retrieval evaluation — `EvaluationService` with KB-scoped case management (add/list/delete) and `run(kbId, {k, limit})` metric runs over the hybrid retriever (per-case latency, cross-KB leakage detection); pure metrics module (recall@K, precision@K, f1@K, percentiles, empty-rate, aggregate report); migration `019_rag_eval_cases_extend.sql` (KB FK cascade, distractor chunks, expected pages, created_by); admin endpoints `POST/GET /rag/eval-cases`, `DELETE /rag/eval-cases/:id`, `POST /rag/evaluate`
- Phase 3 (Production RAG): versioned Qdrant collections + background reindex — versioned collection names derived from the embedding provider, `CollectionResolver` with env override (`QDRANT_COLLECTION_VERSION`) and persisted active version (`rag_index_state`, migration `018_rag_index_state.sql` seeded `legacy` for backward compatibility), `ReindexService` with `rag-reindex` BullMQ worker (old index retained until switch, then superseded points purged), admin-only `POST /rag/reindex` endpoint
- Phase 3 (Production RAG) foundation: explicit ingestion state machine (14 states, retry history, progress), SHA-256 content deduplication, `EmbeddingProvider` interface for pluggable embedding backends, and hybrid retrieval (Qdrant dense + PostgreSQL FTS lexical + Reciprocal Rank Fusion with min-score, dedup and source diversity) on `POST /knowledge-bases/:id/search` (`mode=dense|lexical|hybrid`); migration `017_rag_pipeline.sql` (ingestion state columns, chunk hashes, GIN tsvector index, `rag_eval_cases`)
- Academic structure module (Phase 2 Studyield Core): school profile (country, board, school, grade, academic year), subjects with colour/icon, chapters, topics with learning objectives, exams and exam portions; editable CBSE Grade 9 preset seeded per user; `AcademicsPage` with Profile/Subjects/Exams tabs (all 15 locales)
- Open-source release under GNU Affero General Public License v3.0 (AGPL-3.0)
- Comprehensive CONTRIBUTING.md guide
- SECURITY.md with responsible disclosure policy
- CODE_OF_CONDUCT.md (Contributor Covenant v2.1)
- GitHub issue templates (bug reports, feature requests)
- GitHub pull request template
- Root-level `docker-compose.yml` for one-command self-hosting
- Development startup script (`start.sh`)
- CI workflow for pull request validation (lint, type-check, build)
- Dependabot configuration for automated dependency updates
- `.prettierrc` and `.editorconfig` for consistent code formatting
- NOTICE file with third-party attribution
- CHANGELOG.md following Keep a Changelog format
- DOMPurify sanitization for user-generated HTML content
- React ErrorBoundary component for graceful error handling

### Changed
- Replaced all hardcoded credentials with environment variable placeholders
- Replaced proprietary license with GNU Affero General Public License v3.0 (AGPL-3.0)
- Updated `.gitignore` with comprehensive patterns
- Improved `.env.example` files with clear documentation and grouped sections
- Made all URLs configurable via environment variables
- Fixed CORS gateway fallback from wildcard `*` to localhost defaults
- Replaced console.error with NestJS Logger service
- Removed token debug logging from WebSocket auth guard
- Disabled production sourcemaps in Vite build

### Removed
- Proprietary INFO INLET license
- Hardcoded API keys and secrets from example files
- Hardcoded server IPs from documentation
- Token substring logging from authentication flows
- Production URL fallbacks from frontend config
- `.DS_Store` files from version control

### Security
- Removed all real credentials from `.env.example` files
- Removed production `.env` files from git tracking
- Added DOMPurify to sanitize dangerouslySetInnerHTML content (XSS prevention)
- Fixed CORS wildcard fallback to restrict origins
- Removed sensitive token logging from WebSocket guard

## [0.1.0] - 2025-02-05

### Added
- Initial AI-powered learning platform
- NestJS backend with 27 modules and 120+ REST endpoints
- React 19 frontend with Vite, Tailwind CSS, and Radix UI
- 6 killer AI features:
  - Exam Clone: upload past exams, generate practice questions
  - Multi-Agent Problem Solver: analysis, solution, verification agents
  - Knowledge Graph Visualization: entity and relationship extraction
  - Teach-Back Evaluation: Feynman technique with AI feedback
  - Deep Research Mode: RAG + web search with structured reports
  - Code Sandbox: secure Python execution with scientific libraries
- Knowledge base with document processing and semantic search (RAG)
- AI-generated quizzes and SRS flashcards
- Real-time chat with citations from uploaded documents
- Learning paths with AI-generated study routes
- Stripe subscription billing
- JWT + OAuth authentication (Google, Apple)
- PostgreSQL database with 27 tables (raw SQL, no ORM)
- Redis caching and BullMQ job queue
- Qdrant vector database for semantic search
- ClickHouse analytics
- Cloudflare R2 file storage
- Socket.io real-time WebSocket updates (6 namespaces)
- i18n support for 12 languages
- GitHub Actions CI/CD pipeline
- Production deployment with PM2 and Nginx
